<!--index.wxml-->
<import src="../template/template.wxml"/>
<view class="container">
     <template is="{{product_list.length > 0 ? 'cart-goods-list' : 'no-goods'}}" data="{{...product_list}}"></template>
</view>

